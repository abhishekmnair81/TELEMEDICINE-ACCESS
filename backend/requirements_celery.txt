# Required packages for Celery-based email reminders
# Install with: pip install -r requirements.txt

# Core Django packages (already installed)
Django>=5.2
djangorestframework
djangorestframework-simplejwt
django-cors-headers
python-dotenv
daphne
channels

# Celery and related packages
celery>=5.3.0
redis>=5.0.0
django-celery-beat>=2.5.0
